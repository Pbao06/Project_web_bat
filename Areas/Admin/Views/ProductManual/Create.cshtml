@model Getdata1.Models.Product
@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "New Product";
}

@section Styles {
    <link rel="stylesheet" href="~/css/Use_web/Notification.css" />
    <style>
        .text-danger {
            color: #ff3b30;
            font-size: 0.85rem;
            margin-top: 4px;
            display: block;
        }
        .my-input.input-validation-error {
            border-color: #ff3b30;
        }
    </style>
}

<main>
    <div id="toast-container"></div>
    <div class="head-title">
        <div class="left">
            <h1>New Product</h1>
            <ul class="Breakcrumb">
                <li><a href="#">Inventory</a></li>
                <li><i class='bx bx-chevron-right'></i></li>
                <li><a class="active" href="#">Add Item</a></li>
            </ul>
        </div>
    </div>

    <div class="table-data">
        <div class="order">
            <div class="head">
                <h3>Product Specifications</h3>
            </div>
            
            <form asp-action="Create" method="post" enctype="multipart/form-data" id="createProductForm">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                
                <div class="form-section-title">
                    <h4>General Information</h4>
                </div>

                <div class="form-container" style="display: grid; grid-template-columns: 1fr 1fr; gap: 32px;">
                    <div class="form-group">
                        <label asp-for="Name">Name <span class="text-danger" style="display:inline">*</span></label>
                        <input asp-for="Name" class="my-input" placeholder="e.g. Yonex Astrox 100ZZ">
                        <span asp-validation-for="Name" class="text-danger"></span>

                        <label asp-for="Price">Price (đ) <span class="text-danger" style="display:inline">*</span></label>
                        <input asp-for="Price" class="my-input" placeholder="0">
                        <span asp-validation-for="Price" class="text-danger"></span>

                        <label asp-for="Brand">Brand <span class="text-danger" style="display:inline">*</span></label>
                        <input asp-for="Brand" class="my-input" placeholder="e.g. Yonex">
                        <span asp-validation-for="Brand" class="text-danger"></span>
                        
                        <label asp-for="CategoryId">Category <span class="text-danger" style="display:inline">*</span></label>
                        <select asp-for="CategoryId" id="CategoryId" asp-items="@(ViewData["Category"] as SelectList)" class="my-input">
                            <option value="">Select Category</option>
                        </select>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Stock">Inventory Level <span class="text-danger" style="display:inline">*</span></label>
                        <input asp-for="Stock" class="my-input" placeholder="0">
                        <span asp-validation-for="Stock" class="text-danger"></span>

                        <label asp-for="ImageFile">Cover Image</label>
                        <input asp-for="ImageFile" type="file" class="my-input">
                        
                        <label>Gallery Images</label>
                        <input asp-for="GalleryFiles" type="file" class="my-input" multiple>

                        <label asp-for="Description">Description</label>
                        <textarea asp-for="Description" class="my-input" rows="4"></textarea>
                    </div>
                </div>

                <div id="specs-container">
                    <div class="form-section-title">
                        <h4>Technical Details</h4>
                    </div>

                    <div id="racket-specs" class="spec-section" style="display: none; grid-template-columns: repeat(3, 1fr); gap: 20px; padding: 0 24px;">
                        <div class="form-group">
                            <label asp-for="Stiffness">Stiffness</label>
                            <input asp-for="Stiffness" class="my-input" placeholder="e.g. Cứng, Trung bình">
                            <small class="form-text">Phân tách bằng dấu phẩy nếu có nhiều loại</small>
                        </div>
                        <div class="form-group">
                            <label asp-for="Weight">Weight/Grip</label>
                            <input asp-for="Weight" class="my-input" placeholder="e.g. 3U, 4U">
                            <small class="form-text">Phân tách bằng dấu phẩy nếu có nhiều loại</small>
                        </div>
                        <div class="form-group">
                            <label asp-for="BalancePoint">Balance Point</label>
                            <input asp-for="BalancePoint" class="my-input">
                        </div>
                        <div class="form-group">
                            <label asp-for="RecommendedTension">Tension</label>
                            <input asp-for="RecommendedTension" class="my-input">
                        </div>
                        <div class="form-group">
                            <label asp-for="GripSize">Grip</label>
                            <input asp-for="GripSize" class="my-input" placeholder="e.g. G5, G6">
                            <small class="form-text">Phân tách bằng dấu phẩy nếu có nhiều loại</small>
                        </div>
                        <div class="form-group">
                            <label asp-for="Length">Length</label>
                            <input asp-for="Length" class="my-input">
                        </div>
                    </div>

                    <div id="shoe-specs" class="spec-section" style="display: none; grid-template-columns: repeat(2, 1fr); gap: 20px; padding: 0 24px;">
                        <div class="form-group">
                            <label asp-for="Size">Size</label>
                            <input asp-for="Size" class="my-input" placeholder="e.g. 39, 40, 41">
                            <small class="form-text">Phân tách bằng dấu phẩy nếu có nhiều size</small>
                        </div>
                    </div>

                    <div id="shuttle-specs" class="spec-section" style="display: none; grid-template-columns: repeat(2, 1fr); gap: 20px; padding: 0 24px;">
                        <div class="form-group">
                            <label asp-for="Speed">Speed</label>
                            <input asp-for="Speed" class="my-input" placeholder="e.g. 76, 77">
                            <small class="form-text">Phân tách bằng dấu phẩy nếu có nhiều loại</small>
                        </div>
                    </div>

                    <div id="general-specs" class="spec-section" style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; padding: 24px;">
                        <div class="form-group">
                            <label asp-for="Color">Color</label>
                            <input asp-for="Color" class="my-input" placeholder="e.g. Red, Blue, Black">
                            <small class="form-text">Phân tách bằng dấu phẩy nếu có nhiều màu</small>
                        </div>
                        <div class="form-group">
                            <label asp-for="Material">Material</label>
                            <input asp-for="Material" class="my-input">
                        </div>
                        <div class="form-group">
                            <label asp-for="Origin">Origin</label>
                            <input asp-for="Origin" class="my-input">
                        </div>
                        <div class="form-group">
                            <label asp-for="Warranty">Warranty</label>
                            <input asp-for="Warranty" class="my-input">
                        </div>
                    </div>
                </div>

                <div class="form-footer">
                    <button type="submit" class="btn-save-product">
                        <i class='bx bx-check'></i>
                        <span>Publish Product</span>
                    </button>
                    <a asp-action="Index" class="btn-cancel">Discard Changes</a>
                </div>
            </form>
        </div>
    </div>
</main>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function showToast(message, type = 'success') {
            const container = document.getElementById('toast-container');
            if (!container) return;

            const toast = document.createElement('div');
            toast.className = `toast-item ${type}`;

            let icon = 'bx-check-circle';
            if (type === 'error') icon = 'bx-x-circle';
            if (type === 'warning') icon = 'bx-error-circle';

            toast.innerHTML = `
                <div class="toast-content">
                    <i class='bx ${icon} toast-icon'></i>
                    <span class="toast-message">${message}</span>
                </div>
                <span class="toast-close" onclick="this.parentElement.remove()">×</span>
            `;
            container.appendChild(toast);

            setTimeout(() => {
                toast.style.animation = 'slideOut 0.3s ease forwards';
                setTimeout(() => toast.remove(), 300);
            }, 5000);
        }

        // Form submission confirmation
        document.getElementById('createProductForm').onsubmit = function(e) {
            e.preventDefault();
            const form = this;

            // Kiểm tra validation client-side
            if (!$(form).valid()) {
                showToast("Vui lòng điền đầy đủ các thông tin bắt buộc!", "error");
                return false;
            }

            showGlobalModal({
                title: 'Xác nhận thêm sản phẩm',
                content: 'Bạn có chắc chắn muốn thêm sản phẩm mới này vào hệ thống?',
                confirmText: 'Thêm sản phẩm',
                cancelText: 'Hủy',
                onConfirm: function() {
                    form.submit();
                }
            });
        };

        document.addEventListener('DOMContentLoaded', function() {
            const categorySelect = document.getElementById('CategoryId');
            
            function updateFields() {
                const selectedOption = categorySelect.options[categorySelect.selectedIndex];
                const categoryText = selectedOption ? selectedOption.text.toLowerCase() : '';
                
                document.getElementById('racket-specs').style.display = 'none';
                document.getElementById('shoe-specs').style.display = 'none';
                document.getElementById('shuttle-specs').style.display = 'none';
                
                if (categoryText.includes('racket') || categoryText.includes('vợt')) {
                    document.getElementById('racket-specs').style.display = 'grid';
                } else if (categoryText.includes('shoe') || categoryText.includes('giày') || categoryText.includes('balo') || categoryText.includes('túi')) {
                    document.getElementById('shoe-specs').style.display = 'grid';
                } else if (categoryText.includes('shuttle') || categoryText.includes('cầu')) {
                    document.getElementById('shuttle-specs').style.display = 'grid';
                }
            }

            categorySelect.addEventListener('change', updateFields);
            updateFields();

            // Hiển thị thông báo từ TempData nếu có
            @if (TempData["Notification"] != null)
            {
                <text>
                const notification = @Html.Raw(TempData["Notification"]);
                if (notification) {
                    showToast(notification.Message || notification.message, notification.Type || notification.type);
                }
                </text>
            }
        });
    </script>
}
